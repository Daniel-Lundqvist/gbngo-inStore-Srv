{
  "_comment": "MCP config for Claude Code - enables Playwright browser automation for visual testing. See CLAUDE.md 'Visual Testing with Playwright MCP' section.",
  "mcpServers": {
    "playwright": {
      "command": "cmd",
      "args": ["/c", "npx", "@playwright/mcp@latest", "--output-dir", ".tests/screenshots"],
      "env": {}
    }
  }
}
